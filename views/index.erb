<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Görsel İşleme</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  
  <div class="container py-5">
    <div class="upload-card shadow-lg p-4">
      <h1 class="text-center mb-4">📷 Görsel Yükle ve İşlem Seç</h1>
      <form action="/upload" method="post" enctype="multipart/form-data" class="form-content" id="uploadForm">
        
        <!-- Dosya Yükleme -->
        <div class="mb-4">
          <label for="file" class="form-label fw-bold">Görsel Seç</label>
          <input type="file" name="file" id="file" class="form-control" accept="image/*" required>
          <div class="preview mt-3 text-center">
            <img id="previewImage" src="#" alt="Önizleme" class="img-fluid rounded shadow-sm d-none">
          </div>
        </div>

        <!-- İşlem Seçenekleri -->
        <div class="mb-4">
          <label class="fw-semibold mb-2">İşlem Türü</label>
          <div class="form-check hoverable">
            <input type="radio" name="action" value="remove_bg" id="remove_bg" class="form-check-input">
            <label for="remove_bg" class="form-check-label">🪄 Arka plan sil</label>
            <small class="form-text text-muted">Arka planı kaldırmak için Remove.bg API'si kullanılır.</small>
          </div>
          

          <div class="form-check hoverable">
            <input type="radio" name="action" value="convert_jpg" id="convert_jpg" class="form-check-input">
            <label for="convert_jpg" class="form-check-label">🖼 JPG formatına çevir</label>
            <small class="form-text text-muted">JPG formatı daha düşük dosya boyutu sunar.(Yakında)</small>
          </div>

          <div class="form-check hoverable">
            <input type="radio" name="action" value="convert_png" id="convert_png" class="form-check-input">
            <label for="convert_png" class="form-check-label">🖼 PNG formatına çevir</label>
            <small class="form-text text-muted">PNG formatı şeffaf arka plan desteği sunar.(Yakında)</small>
</div>
      
        </div>

      
        <div class="text-center">
          <button type="submit" class="btn btn-gradient px-4 py-2">🚀 Yükle ve İşle</button>
        </div>
        <div class="text-center mt-3">
          <small class="text-muted">Yükleme ve işlem süreci birkaç saniye sürebilir.</small>
        </div>

      </form>
    </div>
  </div>

  <script>

    document.getElementById("file").addEventListener("change", function(event) {
      const file = event.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function(e) {
          const img = document.getElementById("previewImage");
          img.src = e.target.result;
          img.classList.remove("d-none");
        }
        reader.readAsDataURL(file);
      }
    });
  </script>
</body>
</html>
